import React from "react";
import './button.css';

export default function Button({}) {
    return (
        <div className="rating">
        <button className="rate">Rate shop</button>
        <p className="not">not working yet</p>
        </div>
    );
}